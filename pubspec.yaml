name: alzheimer_classifier
description: Clasificador Alzheimer con CNN - Examen Parcial
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.0.0
  image_picker: ^1.0.7    # Seleccionar imagen de galería / cámara
  http: ^1.2.0             # Peticiones HTTP al backend Flask
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
